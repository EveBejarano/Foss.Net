@model IEnumerable<Events.Models.Excursion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.City.CityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Transport.TransportType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Destiny)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EventDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.City.CityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Transport.TransportType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Destiny)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EventDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExcursionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExcursionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExcursionID })
        </td>
    </tr>
}

</table>
