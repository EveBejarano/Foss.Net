@model IEnumerable<FlightsAPI.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employ_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DNI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Emp_Occupation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employ_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DNI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Emp_Occupation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEmploy }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEmploy }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEmploy })
        </td>
    </tr>
}

</table>
