@model IEnumerable<FlightsAPI.Models.FilghtPlace>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Place_Owner_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place_Owner_DNI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FP_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CommercialFlight.Flight_To)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Place_Owner_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Place_Owner_DNI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FP_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CommercialFlight.Flight_To)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.numPlace }) |
            @Html.ActionLink("Details", "Details", new { id=item.numPlace }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.numPlace })
        </td>
    </tr>
}

</table>
